DATABASE_URL="postgresql://heyday:your_db_password@localhost:5432/heyday_db?schema=public"
GEMINI_API_KEY="your_gemini_api_key_here"

# When running fully in Docker (docker compose up), DATABASE_URL is set automatically.
# Only GEMINI_API_KEY needs to be provided as an environment variable or in a .env file.
